__pycache__/
*.py[cod]
*.egg-info/
.venv/
.env
.mypy_cache/
.ruff_cache/
.pytest_cache/
dist/
build/
bench/results.json
.coverage
*.sqlite
# Cross-run budget ledger (per ADR 0005). Generated under
# config/.budgeteer/ when [cross_run].cap_usd is set in the active
# policy.yaml. The data is per-deployment state, not source.
.budgeteer/
*.db
# Live-bench per-task cap ledgers (per AB-6). Recreated on every run.
bench/live/.ledger/
# v0.3 four-arm SWE-bench harness output (smoke + live runs).
bench/results/
